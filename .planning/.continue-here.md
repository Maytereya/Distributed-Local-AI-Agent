---
context: default
phase: messengers-router-refactor
task: 8
total_tasks: 17
status: ready_for_stage_8
last_updated: 2026-04-17T12:14:20.817Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Remote eval not yet revalidated — local tests are green through Stage 7, but server-side Stage 5/full eval was not rerun after the historical timeout.
- [ ] CONSTRAINT: Local Nayka cache is date-sensitive — if `20260417` cache files are missing or empty, pytest can fail for environment reasons instead of router logic.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Local green treated as remote parity | Stage 5 corpus timeout was server-side; local pytest is not a substitute for remote eval readiness. | advisory | Keep coding verification local, but treat remote Stage 5/full eval as a separate explicit follow-up once branch state is pushed. |
| Mixing unrelated user files into stage commits | The working tree contains `.gitignore`, untracked docs, `.DS_Store`, and other non-stage files. | blocking | Stage only the exact files for the current stage; inspect `git diff --cached --stat` before every commit. |

<current_state>
We are on `/Users/maxten/Dev/Distributed-Local-AI-Agent2`, branch `refactor/core`.

Stage 7 is complete and committed:
- `2833bf7` — `refactor(router): collapse overlapping state-clear helpers`

Stages already completed on this branch:
- `36cfe69` — Stage 3
- `da77de9` — Stage 4
- `9f9483b` — Stage 5
- `d026414` — Stage 6

Next target is **Stage 8** from `docs/messengers_router_refactor_plan.md`: extract test-result helpers into `messengers_router/services/lab_tests.py`.
</current_state>

<completed_work>

Completed Tasks:
- Task 0-1: baseline normalization, Stage 0/1 cleanup, and Stage 5 diagnostics checkpoint — Done
- Task 2: `guardrail_precheck` made synchronous — Done
- Task 3: swallowed exceptions now log instead of disappearing — Done
- Task 4: evidence handoff check routed through policies helper — Done
- Task 5: duplicate `reset_appointment_runtime_state` unified with hard-reset behavior — Done
- Task 6: hardcoded service handoff strings centralized in `HANDOFF_REASON_MATRIX` — Done
- Task 7: overlapping state-clear helpers collapsed into canonical named helpers — Done
</completed_work>

<remaining_work>

- Task 8: extract test-result helpers from `services_legacy.py` into `services/lab_tests.py`
- Task 9: split `quick_fill_core_entities` by entity domain
- Later stages: continue only after Stage 8 is green and committed
</remaining_work>

<decisions_made>

- Stage 5 used **Option A**: hard-reset appointment runtime and clear `dialog` to prevent stale APPOINTMENT leaks.
- Stage 6 used **Option B**: add per-domain handoff keys to `HANDOFF_REASON_MATRIX` and preserve user-facing wording exactly.
- Stage 7 used **Option B**: small named clear helpers with a shared private core instead of one large reason-switch function.
- Renderer "not found" texts were intentionally left outside `HANDOFF_REASON_MATRIX`.
</decisions_made>

<blockers>
- Remote parity blocker: `RUN_ID=1776366965`, `04_stage5_golden_corpus` timeout remains the last known server-side issue.
- Environment blocker: local pytest requires hydrated `20260417` Nayka cache files in `agent_logic_2/nayka_api/apidata/`.
</blockers>

## Required Reading (in order)
1. `/Users/maxten/Dev/Distributed-Local-AI-Agent2/docs/messengers_router_refactor_plan.md` — authoritative stage list and current status through Stage 7
1. `/Users/maxten/Dev/Distributed-Local-AI-Agent2/messengers_router/services/lab_tests.py` — Stage 8 destination module
1. `/Users/maxten/Dev/Distributed-Local-AI-Agent2/messengers_router/services_legacy.py` — current source of test-result helpers to extract
1. `/Users/maxten/Dev/Distributed-Local-AI-Agent2/tests/test_messenger_services.py` — likely Stage 8 regression surface

## Critical Anti-Patterns (do NOT repeat these)
- Do not fold unrelated local files into Stage 8 commits. Stage only the service/test-result extraction files.
- Do not treat missing dated Nayka cache files as a product bug before hydrating the cache.

## Infrastructure State
- Python env: `/Users/maxten/Dev/Distributed-Local-AI-Agent2/venv`
- Branch state: `refactor/core` is ahead of `origin/refactor/core` by 4 commits
- Local verification already green through Stage 7:
  - `ruff check ...` on Stage 7 files
  - `pytest tests/test_router_flow_override.py -q` → `171 passed`
  - `pytest tests/ -x -q --ignore=tests/eval` → `555 passed, 3 warnings`

<context>
The branch is in good shape for Stage 8. The main thing to preserve is stage atomicity: Stage 8 should be a pure extraction/move of test-result helpers into `services/lab_tests.py`, with call-site rewiring and no incidental refactors. Keep the current practice: write a failing targeted test first, move the smallest coherent slice, rerun focused tests, then rerun the full local pytest suite.
</context>

<next_action>
Start with: read the Stage 8 section in `docs/messengers_router_refactor_plan.md`, then inspect `messengers_router/services/lab_tests.py` and grep the exact test-result helper names in `messengers_router/services_legacy.py` before writing the first failing Stage 8 test.
</next_action>
