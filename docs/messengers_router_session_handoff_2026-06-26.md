# Хендофф сессии 2026-06-26 → продолжение дорожной карты (П7-П9)

> **Зачем:** контекст сессии кончается. Реализуем 9-пунктовую дорожную карту из
> `docs/architecture_review_2026-06-26.md` §7 (владелец одобрил все 9, модель Fable).
> **П1-П6 сделаны** (см. §1). Новому чату: продолжить с **П7-П9** (§3), соблюдая
> конвенции §4. Ветка **release**, НЕ переключать.

## 0. Прочитать первым (порядок)
1. этот файл → 2. `docs/architecture_review_2026-06-26.md` (разбор+карта; §7 — суть)
→ 3. `docs/baseline_2026-06-26.md` (точка отката/сравнения: тег `baseline-2026-06-26`,
метрики, golden-пробы, латентность rule 1.6с / llm 17.3с p50) → 4. bug-log верхние
записи (FEAT-П5, FEAT-П6, BUG-2026-06-23-01).

## 1. Сделано в этой сессии (все коммиты на release, запушено до П4 включительно)

| П | Коммиты | Что |
|---|---|---|
| фикс | `f2f2d58` | architecture_review + baseline docs; тег `baseline-2026-06-26` (запушен) |
| П1 | `6ad3a08` | удалена «agent_logic_1/ obsolete/» (пароль Наяки medserver-test + Tavily key в tracked-коде). **Владелец: ротация обоих у Наяки/Tavily — ЕЩЁ НЕ подтверждена.** Решение: ротация вместо filter-repo |
| П2 | `aa10f34`,`24a999e`,`444bb80` | CI job (ruff+7 hermetic тест-файлов, requirements-ci.txt, закоммичен test_security_probe.py); .DS_Store из git + .gitignore (снята мина agent_logic_2/config.py); docs/archive/ (14 доков) + баннер на мартовский API-аудит; удалён claude-worktree. **Проверить: первый прогон CI на GitHub Actions зелёный?** |
| П3 | `d5bb6b0` | stage_timings → debug-ответ (после деплоя); scripts/measure_latency_baseline.py; baseline: rule p50=1.59с, llm p50=17.3с (docs/latency_baseline_2026-06-26.json) |
| П4 | `8cf39ca` | docs/ops_ollama_quant_procedure.md — **исполняет владелец на сервере** (квант q8/q4 + NUM_PARALLEL, A/B через eval) |
| П5 | `aa4f4ef` | LLM-нормализация услуг (вариант B): `services/_service_normalizer.py`, интеграция в match_catalog_service (fallback-only → верификация → fuzzy-кандидат → confirm-флоу). Промпт `service_normalizer` ×2 локации. Kill-switch `[MESSENGER_ROUTER] llm_service_normalization=false`. 17 тестов |
| П6 | `4ebe6a1` + **hotfix следом** («fix(lab): распаковка (items, unrecognized)…») | сплиттер `\n`/`•`; сегментация сплошного списка ПО КАТАЛОГУ с гардами (анти-глотание соседей, анти-подмена B1≠B12, алиасы в обход гардов); честный «Не распознал: …». 8 тестов `test_multi_price_cart.py`. ⚠️ Инцидент: `4ebe6a1` ушёл при КРАСНОМ гейте (10 failed — пропущен второй вызывающий `lab_tests.py:162` при смене сигнатуры на tuple); hotfix закоммичен через цепочку с жёстким стопом по exit-коду. Урок в памяти `feedback-gate-exit-code-discipline`: коммит ТОЛЬКО через `pytest && git commit`, греп вызывающих по всему репо при смене сигнатур |

Все под полным гейтом (последний: 1158 passed + П6). Bug-log: FEAT-2026-06-26-П5/П6.

## 2. ⚠️ Открытые хвосты сделанного (проверить/донести)
- **Ротация паролей** (П1): владелец сообщает Сергею/Наяке (пароль «ai» к
  medserver-test) — до ротации секрет остаётся в git-ИСТОРИИ (решение: не переписывать).
- **CI первый прогон** — глянуть Actions; если красный, скорее всего пин из
  requirements-ci.txt на ubuntu — поправить версию/убрать пакет.
- **П5/П6 живьём НЕ проверены** (LLM замокан в тестах): после деплоя — прод-пробы:
  «сколько стоит коагулограмма» → confirm «Вы имели в виду Гемостазиограмма…?»;
  «сахар в крови» → Глюкоза; список «ОАК ОАМ ОБЩИЙ БЕЛОК ГЛЮКОЗА» → все позиции
  с ценами; «ОАК, ферритин, КВАНТОВЫЙ АНАЛИЗ» → цены + «Не распознал: КВАНТОВЫЙ…».
  Затем **полный remote eval** (сравнить с baseline-таблицей).
- **B1+B6 обе цены** — гард подмены отключает сегментацию (B1→B12 резолв-фаззи) →
  single-путь. Follow-up: S4 (LLM-экстракция списка) или digit-точный tie-break.
- Pre-existing quirk: `resolve("анти-тг")`→«Анти-ХГЧ» (подмена в самом resolve) —
  отдельная грань BUG-2026-06-02-07, не чинилась.

## 3. TODO: П7-П9 (детальные спеки)

### П7 — Resilience Phase 2: gather в find_doctor_schedule (средний риск)
Последовательный fanout расписания (филиалы × дни; 7.2с Трубин). План УЖЕ написан:
`docs/superpowers/plans/2026-06-06-resilience-degraded-mode-phase1.md`, раздел
«Phase-2 follow-ups». Делать: `asyncio.gather` по независимым fetch'ам в
`services/doctors.py` (`find_doctor_schedule`/`doctors_schedule_week`, ~строки
680-910) + `asyncio.Semaphore(3-4)` (не душить medserver) + сохранение ПОРЯДКА
результатов (сортировка после gather, не по прибытию). Тесты: существующие
schedule-тесты + новый инвариант «порядок филиалов/дней детерминирован». Проверка:
eval stage 3 (APPT_* сценарии) + замер (пробой расписания Трубина до/после).

### П8 — Embedding-слой синонимов: ОФЛАЙН-валидация (только отчёт!)
Скрипт-харнесс (scratchpad или tools/): проиндексировать 3675 serviceName
(инфра: `agent_logic_1/embedding_filtration.py` — torch-эмбеддер уже в репо;
Chroma НЕ обязателен — численно в памяти). Прогнать: позитив-пары из bug-log
(забор→взятие, электромиография→ЭМГ, сахар→глюкоза, коагулограмма→гемостазиограмма,
са-125→CA-125…) + негативы (парковка, запишите к врачу, ФИО) → recall@1/@5 +
false-positive rate. РЕЗУЛЬТАТ = отчёт владельцу; включение в прод — отдельное
решение (это слой 3 воронки: карта → LLM-П5 → embeddings).

### П9 — Faithfulness: «больничный лист» + промпт-гард + eval-пробы (низкий риск)
Контекст: 25.06 бот галлюцинировал «клиника НЕ оформляет больничные» (оформляет!
любой врач на приёме при показаниях, Самара+область, выдаёт оператор через
программу БЛ — слова Сергея). **Решение владельца: пока факты не подтверждены —
БЛ-интент → ПЕРЕВОД НА ОПЕРАТОРА** (не факт-ответ!). Делать:
1. `SICK_LEAVE_PATTERNS` в policies.py (`больничн\w*`, `лист\w*\s+нетрудоспособ\w*`,
   ReDoS-safe) + `detect_sick_leave_intent`; ветка в classifier.py ПО ОБРАЗЦУ
   `detect_website_help_intent` (BUG-2026-06-09-06: clarify/handoff-ветка ПЕРЕД
   другими правилами; тут → handoff=True «Вопрос про больничный лист уточню у
   оператора — соединяю» ЛИБО оффер оператора). Класс-тест: detect + rule_decision
   (больничный→оператор, НЕ галлюцинация; негативы: «больничная улица» (адрес!
   есть филиал на ул. Больничная 44Г в Красном Яру — анти-FP тест обязателен),
   «лист назначений»).
2. Промпт-гард в renderer_patient*/messenger_final_answer (×2 локации, КОРОТКО):
   «О правилах/услугах клиники, которых нет в Данные, не утверждай — предложи
   оператора» (укладывается в существующий блок конфиденциальности).
3. 2-3 faithfulness-пробы в `eval_suite/critical_cases.jsonl` (по образцу
   CRIT_SECURITY_*): «занимаетесь ли больничными?» forbidden_any=["не занимается",
   "не оформляет"], required_any=["оператор"]; «делаете ли МРТ колена?» (клиника
   НЕ делает МРТ — CRIT_STOP_UNSUPPORTED_MRI уже есть, проверить покрытие).
Когда Сергей подтвердит текст фактов → заменить оператор-ответ на статический
факт-ответ (черновик в истории: «оформляем, любой врач на приёме при показаниях…»,
+ решить про исключение «пластические операции без БЛ» из ватсап-архива 113.txt).

## 4. Конвенции (НЕ нарушать; полные — в памяти проекта и §5 handoff-06-11)
- Ветка `release`; атомарные коммиты `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`;
  гейт ПЕРЕД коммитом: `./venv/bin/python -m pytest -q -p no:cacheprovider` (полный,
  ~4-7 мин, фоном) + `ruff check` (touched). TDD: класс-инвариант, RED→GREEN.
- Промпты в ДВУХ локациях (`messengers_router/prompts/*` + `app_data/prompts/mr_*`, diff identical).
- NLU прод = legacy_v2: rule не-None короткозамыкает LLM → чинить правило; rule=None → промпт+eval.
- **FreeTalk (`src/localragagent/`) НЕ трогать** — Владимир (память project-freetalk-ownership).
- config.ini вне git, вручную; **config.PY — в git** (не путать). При изменении
  структуры config.ini — сказать владельцу (передаёт Владимиру руками).
- Прод SSH с Mac нет; эндпоинт `http://172.16.0.16/api/messenger-generate-once` с Mac доступен.
- Регексы policies.py — ReDoS-safe (test_security_probe гоняет).
- Не дезинформировать НИКОГДА: честный degraded/оператор > выдумка.
- git: с коммитами Владимира — merge, не rebase; `git config pull.rebase false`.

## 5. Рекомендованный порядок нового чата
1. `git log --oneline -15` + `git status` (убедиться, где HEAD; всё ли запушено).
2. П9 (быстрый, высокая ценность — дезинформация) → П7 (готовый план) → П8 (отчёт).
3. После П7-П9: владелец ребилдит прод + remote eval + прод-пробы §2 + П4-квант.
4. Обновить этот handoff → при исчерпании контекста — новый.
