version: "3.8"
services:
  bookworm-agent:
    build: .
    container_name: bookworm-agent
    ports:
      - "7860:7860"
    networks:
      - bridge  # Используем ту же сеть, что и существующие контейнеры

networks:
  bridge:
    external: true  # Говорим, что сеть уже существует